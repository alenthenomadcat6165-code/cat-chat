import {env} from 'cloudflare:workers';import {bytes,clearSession,hash,json,me,session,setup} from '../../../lib/store';
export async function GET(){await setup();return json({user:await me()})}
export async function POST(req:Request){await setup();const b=await req.json() as {action:string;username?:string;name?:string;password?:string};if(b.action==='logout')return json({ok:true},200,{'Set-Cookie':clearSession()});const username=(b.username??'').trim().toLowerCase(),password=b.password??'';if(!/^[a-z0-9._-]{3,20}$/.test(username))return json({error:'Use 3–20 letters, numbers, dots, dashes, or underscores.'},400);if(password.length<8)return json({error:'Password needs at least 8 characters.'},400);let user:any;if(b.action==='register'){const name=(b.name??'').trim();if(name.length<2||name.length>30)return json({error:'Enter a display name.'},400);const salt=bytes(16),passwordHash=await hash(password,salt);try{const count=await env.DB.prepare('SELECT COUNT(*) AS n FROM users').first<{n:number}>(),role=Number(count?.n??0)===0?'creator':'member',result=await env.DB.prepare('INSERT INTO users(username,name,password_hash,salt,role,created_at) VALUES(?,?,?,?,?,?)').bind(username,name,passwordHash,salt,role,Date.now()).run();user={id:Number(result.meta.last_row_id),username,name,avatarKey:null,role,banned:0}}catch{return json({error:'That username is already used.'},409)}}else{const row=await env.DB.prepare('SELECT id,username,name,avatar_key AS avatarKey,role,banned,password_hash AS passwordHash,salt FROM users WHERE username=?').bind(username).first<any>();if(!row||await hash(password,row.salt)!==row.passwordHash)return json({error:'Username or password is wrong.'},401);if(row.banned)return json({error:'This account was paused by the creator.'},403);user={id:row.id,username:row.username,name:row.name,avatarKey:row.avatarKey,role:row.role,banned:row.banned}}const token=bytes();await env.DB.prepare('INSERT INTO sessions(token,user_id,expires_at) VALUES(?,?,?)').bind(token,user.id,Date.now()+2592000000).run();return json({user},200,{'Set-Cookie':session(token)})}
