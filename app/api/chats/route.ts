import {env} from 'cloudflare:workers';import {json,me} from '../../../lib/store';export async function POST(req:Request){const user=await me();if(!user)return json({error:'Open the site again.'},401);const b=await req.json() as {name?:string;type?:string;memberIds?:number[]},ids=Array.from(new Set((b.memberIds??[]).filter(x=>x!==user.id)));if(b.type==='private'&&ids.length!==1)return json({error:'Choose one person.'},400);if(b.type==='group'&&!ids.length)return json({error:'Choose at least one person.'},400);const name=b.type==='private'?'Private chat':(b.name??'').trim();if(name.length<2)return json({error:'Add a group name.'},400);const r=await env.DB.prepare('INSERT INTO conversations(name,type,created_by,created_at) VALUES(?,?,?,?)').bind(name,b.type,user.id,Date.now()).run(),id=Number(r.meta.last_row_id);await env.DB.batch([user.id,...ids].map(uid=>env.DB.prepare('INSERT INTO conversation_members(conversation_id,user_id) VALUES(?,?)').bind(id,uid)));return json({chat:{id,name,type:b.type}})}
